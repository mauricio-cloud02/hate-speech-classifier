# Data Directory

The dataset is not included in this repository.

Expected dataset location:

- `data/labeled_data.csv`

Required columns in the CSV:

- `tweet` (text)
- `class` (label)

If you are using the common hate-speech Twitter dataset from Kaggle, download it separately and place the CSV at the path above.
